{
  "name": "jintian-chidian",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm.cmd test && node --check src/app.js && node --check src/core.js && node --check scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "start": "node scripts/serve.mjs"
  }
}
